# Hasta dónde llega base-minima.sql.
#
# ⚠ Esto ya NO es la lista de migraciones previas. Antes lo era, y se
# desactualizó las dos veces que se pudo: le faltó la 054 en agosto, se
# arregló, y para cuando llegó la 057 le faltaba la 056. Cada migración nueva
# era una oportunidad de olvidarse, y el banco daba VERDE corriendo una historia
# incompleta.
#
# Ahora `probar.sh` calcula las previas: corre todo lo que está en migrations/
# desde este número (inclusive) hasta la que se está probando (exclusive).
# Agregar una migración no requiere tocar este archivo.
#
# La ÚNICA razón para cambiar este número es que base-minima.sql cambie de
# alcance — o sea, que se lo reescriba para que simule el esquema hasta más
# adelante. Si eso pasa, se cambia acá y en ningún otro lado.

desde: 051
